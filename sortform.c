
#include<stdio.h>
void main()
{
	char name[100],str[50];
	int i,j,count=0;
	printf("enter your name :");
	gets(name);
	for(i=0;name[i]!='\0';i++)
	{
		str[0]==name[0];
		if(name[i]==' ')
		{
			for( j=1;j<=20;j++)
			{
				str[j]==name[i+1];
			}
			count++;
		
		}
	}
		printf(" total words present:%d",count);
		printf("%c",str);
	}
	


